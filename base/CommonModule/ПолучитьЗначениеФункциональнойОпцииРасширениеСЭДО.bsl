
Функция ПолучитьЗначениеФункциональнойОпции(ИмяФункциональнойОпции) Экспорт

	Если Найти(ИмяФункциональнойОпции, "ИспользоватьЭЛН") > 0 Тогда
		Возврат Истина;
	Иначе
		Возврат ПолучитьФункциональнуюОпцию(ИмяФункциональнойОпции);
	КонецЕсли;

КонецФункции

