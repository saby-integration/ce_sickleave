
#Область include_core_base_Helpers_ПрефиксПродукта
#КонецОбласти

&Вместо("МетаданныеВходящихСообщенийСЭДОФСС")
Функция Sedo_МетаданныеВходящихСообщенийСЭДОФСС(
		Организация,
		ДатаСообщений = Неопределено,
		ТипВзаимодействия = Неопределено,
		СвойстваОбменаПоСЭДОЧерезОператора = Неопределено) Экспорт

	Если Sedo_ЭДОСФСС.ПолучитьЗначениеФункциональнойОпции(ПрефиксПродукта() + "ИспользоватьЭЛН") Тогда
		// Возвращаем вместо идентификаторов даты сообщений. В дальнейшем мы выполним СБИС.СписокИзменений
		// и загрузим все входящие сообщения минимально указанной даты.
		Результат = Новый Структура("Выполнено, ОписаниеОшибки, ДанныеСообщений", Истина, "", Новый Массив);
		Результат.ДанныеСообщений.Добавить(
			Новый Структура(
				"Идентификатор, Тип, Получатель, ТребуетсяПодтверждение, Новое",
				ДатаСообщений,
				2,
				Ложь,
				Ложь));
		Возврат Результат;
	Иначе
		Результат = ПродолжитьВызов(Организация, ДатаСообщений,
						ТипВзаимодействия, СвойстваОбменаПоСЭДОЧерезОператора);
		Возврат Результат;
	КонецЕсли;

КонецФункции
